/* =====================================================
   設定ファイル（解禁方法・共通設定はここだけを編集）
   ===================================================== */
const CONFIG = {

  /* --- 解禁方法 ---
     'button' : 管理ページ（admin.html）の「公開する」ボタンで解禁（推奨）
                解禁前はカウントダウンなしの待機画面が表示されます。
     'time'   : 指定時刻になったら自動で解禁（カウントダウン表示） */
  unlockMode: 'button',

  /* --- ボタン方式の設定 --- */
  // 公開フラグの置き場所（リポジトリの status.json を raw URL で参照）
  statusUrl: 'https://raw.githubusercontent.com/kcroquis219-alt/-/main/status.json',
  // 保険: この時刻を過ぎたらボタンに関係なく解禁する（空なら無効）
  // 例: '2026-08-01T14:00:00+09:00'
  failsafeTime: '',

  /* --- 時刻方式の設定（unlockMode:'time' のとき使用） --- */
  unlockTime:  '2026-08-01T13:00:00+09:00',
  unlockLabel: '8月1日 13:00',

  /* --- 卓ごとの上書き（なければ空のままでOK） ---
     例:
     tableOverrides: {
       B: { unlockTime: '2026-08-01T13:15:00+09:00', unlockLabel: '8月1日 13:15' },
       C: { label: '志賀島のみなさまへ' },   // 卓名の表示だけ変えることも可能
     },
  */
  tableOverrides: {},

  /* --- 表示まわり --- */
  heroPhoto: '',   // 会場写真を使う場合は画像ファイル名（例:'marizon.jpg'）。空なら線画のまま
  signature: '二〇二六年八月一日<br>マリゾン福岡にて　新郎・新婦より',

  /* --- サーバー時刻ベースの解禁判定（'time'方式・failsafeTimeで使用) ---
     true にすると、ページ配信サーバー（GitHub Pages等）の時刻を基準に判定します。
     端末の時計を進めても解禁前に見えなくなります。
     取得に失敗した場合（オフライン・ローカルで開いた場合）は端末時計に自動フォールバック。 */
  useServerTime: true,
};
